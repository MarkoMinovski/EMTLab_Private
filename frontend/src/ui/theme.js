import {createTheme} from '@mui/material/styles';

const theme = createTheme({
    palette: {
        mode: 'dark',
        primary: {
            main: "#382F56"
        },
        secondary: {
            main: "#2C2F5B"
        },
    }
})


export default theme;

